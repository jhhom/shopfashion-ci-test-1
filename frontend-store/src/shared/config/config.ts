export const SERVER_PORT = 9090;
const ASSET_SERVER_PORT = 9085;

export const config = {
  SERVER_URL: `http://localhost:${SERVER_PORT}`,
  ASSET_SERVER_URL: `http://localhost:${ASSET_SERVER_PORT}`,
  ASSET_SERVER_PORT,
  WEBHOOK_SERVER_PORT: 9080,
  STORE_FRONT_PORT: 9020,
};
