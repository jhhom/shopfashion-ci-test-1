export type JsonColumns = {
  Orders: {
    address: {
      fullName: string;
      address1: string;
      address2: string;
      city: string;
      state: string;
      postalCode: string;
      mobilePhone: string;
    };
  };
};
